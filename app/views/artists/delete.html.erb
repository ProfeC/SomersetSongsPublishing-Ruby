<% content_for :title do %>
  <%= "Deleting " @artist.name %>
<% end %>

<header class="row">
  <section class="medium-12 column page-title">
    <h1>Artists#delete</h1>
    <%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>
  </section>
</header>

<main class="row">
  <section class="medium-12 columns">
  <p>Find me in app/views/artists/delete.html.erb</p>

  <div class="artists delete">

  <%= form_for(:artist, :url => {:action => 'destroy', :id => @artist.id}) do |f| %>

    <p>Are you sure you want to permanently delete this artist?</p>

    <p class="reference-name"><%= @artist.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Artist") %>
    </div>

  <% end %>

    </div>
  </section>
</main>
